import React from "react";

const Header: React.FC = () => {
  return (
    <header style={{ padding: "16px", background: "#0d6efd", color: "#fff" }}>
      <h1>Dio Bank</h1>
    </header>
  );
};

export default Header;
